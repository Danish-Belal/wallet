export const merchantSignup = async(req:any,rep:any)=>{
     const username= req.username;
     const password= req.password;

     return username;
}
export const merchantSignin = async(req:any,rep:any)=>{
     const username= req.username;
     const password= req.password;

     return username;
}
