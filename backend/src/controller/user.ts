
export const userSignup = async(req:any,rep:any)=>{
     const username= req.username;
     const password= req.password;

     return username;
}
export const userSignin = async(req:any,rep:any)=>{
     const username= req.username;
     const password= req.password;

     return username;
}

